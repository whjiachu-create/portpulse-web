diff --git a/src/components/Analytics.tsx b/src/components/Analytics.tsx
index 3b1c2dd..6a9fd31 100644
--- a/src/components/Analytics.tsx
+++ b/src/components/Analytics.tsx
@@ -1,6 +1,7 @@
 "use client";
 import { useEffect } from "react";
 import Script from "next/script";
+type StrDict = Record<string, string | number | boolean>;

 type GtagArg =
   | ["js", Date]
@@ -35,6 +36,19 @@ export default function Analytics() {
   );
 }

+function send(name: string, params?: StrDict) {
+  if (typeof window === "undefined") return;
+  const w = window as unknown as { gtag?: (...args: GtagArg) => void };
+  if (typeof w.gtag === "function") {
+    w.gtag("event", name, params as Record<string, unknown>);
+  }
+}
+
 function PageEvents() {
   useEffect(() => {
     if (typeof window !== "undefined" && typeof window.gtag === "function") {
-      window.gtag("event", "page_view", {
+      window.gtag("event", "page_view", {
         page_location: window.location.href,
         page_path: window.location.pathname,
       });
       if (window.location.pathname === "/") {
         window.gtag("event", "view_home");
       }
     }

     const click = (e: MouseEvent) => {
       const target = e.target as Element | null;
       const a = target?.closest("a") as HTMLAnchorElement | null;
-      if (!a || typeof window.gtag !== "function") return;
-      if (a.pathname === "/pricing") window.gtag("event", "cta_click", { target: "pricing" });
-      if (a.pathname === "/contact") window.gtag("event", "cta_click", { target: "contact" });
+      if (!a) return;
+      const p = a.pathname || "";
+      // CTA
+      if (p === "/pricing") send("cta_click", { target: "pricing" });
+      if (p === "/contact") send("cta_click", { target: "contact" });
+      // 导航/功能页点击
+      const navTargets = ["/docs", "/coverage", "/product", "/use-cases", "/methods"];
+      if (navTargets.some((x) => p === x || p.startsWith(`${x}/`))) {
+        send("nav_click", { target: p });
+      }
     };

     // 用布尔 capture，便于成对移除
     document.addEventListener("click", click, true);
     return () => document.removeEventListener("click", click, true);
   }, []);

   return null;
 }
diff --git a/src/app/docs/api/page.tsx b/src/app/docs/api/page.tsx
index b2a9f44..a98cd21 100644
--- a/src/app/docs/api/page.tsx
+++ b/src/app/docs/api/page.tsx
@@ -1,6 +1,7 @@
 "use client";
-import Script from "next/script";
-import type { DetailedHTMLProps, HTMLAttributes } from "react";
+import Script from "next/script";
+import { useEffect } from "react";
+import type { DetailedHTMLProps, HTMLAttributes } from "react";

 declare global {
   // web component typing (no 'any' to satisfy eslint)
   namespace JSX {
     interface IntrinsicElements {
       "rapi-doc": DetailedHTMLProps<HTMLAttributes<HTMLElement>, HTMLElement>;
     }
   }
 }

 export default function ApiDocs() {
   const specUrl = "https://docs.useportpulse.com/openapi.json";
+  useEffect(() => {
+    if (typeof window !== "undefined" && typeof (window as any).gtag === "function") {
+      (window as any).gtag("event", "docs_open", { page: "api" });
+    }
+  }, []);
   return (
     <div className="container mx-auto px-2 md:px-4 py-4 md:py-8">
       <Script src="https://unpkg.com/rapidoc/dist/rapidoc-min.js" strategy="lazyOnload" />
       <rapi-doc
         spec-url={specUrl}
diff --git a/src/app/docs/examples/page.tsx b/src/app/docs/examples/page.tsx
index 2f8ee12..8a8b9c1 100644
--- a/src/app/docs/examples/page.tsx
+++ b/src/app/docs/examples/page.tsx
@@ -1,4 +1,5 @@
+import { useEffect } from "react";
 import CodeTabs from "@/components/CodeTabs";

 const BASE = "https://api.useportpulse.com";
 const DEMO = "dev_demo_123";
@@ -28,6 +29,10 @@ const js = `const BASE="${BASE}";
 export const dynamic = "force-static";

 export default function ExamplesPage() {
+  useEffect(() => {
+    if (typeof window !== "undefined" && typeof (window as any).gtag === "function") {
+      (window as any).gtag("event", "docs_open", { page: "examples" });
+    }
+  }, []);
   return (
     <div className="container mx-auto px-4 py-12 md:py-16">
       <h1 className="text-3xl font-semibold tracking-tight">Quickstart & Examples</h1>
diff --git a/src/app/docs/sdk/page.tsx b/src/app/docs/sdk/page.tsx
index 9b2a7f6..1a2b3c4 100644
--- a/src/app/docs/sdk/page.tsx
+++ b/src/app/docs/sdk/page.tsx
@@ -1,3 +1,4 @@
+import { useEffect } from "react";
 import CodeTabs from "@/components/CodeTabs";
 export const dynamic = "force-static";

@@ -27,6 +28,12 @@ const js = `export class PortPulse {
 // const c = new PortPulse("dev_demo_123"); console.log(await c.trend("USLAX",7));`;

 export default function SDKPage() {
+  useEffect(() => {
+    if (typeof window !== "undefined" && typeof (window as any).gtag === "function") {
+      (window as any).gtag("event", "docs_open", { page: "sdk" });
+    }
+  }, []);
+
   return (
     <div className="container mx-auto px-4 py-12 md:py-16">
       <h1 className="text-3xl font-semibold tracking-tight">SDK Samples</h1>
       <p className="mt-2 text-black/60">Minimal clients to get you started. Copy & adapt.</p>
       <div className="mt-6">
         <CodeTabs tabs={[
           { id:"py", label:"Python", language:"python", code: py },
           { id:"js", label:"JavaScript", language:"javascript", code: js },
         ]}/>
       </div>
     </div>
   );
 }